# Opcional: chaves do Supabase embutidas no build (pula a tela de colar URL/chave).
# Copie para .env.local para desenvolvimento, ou configure como variáveis de
# ambiente no Vercel / GitHub Actions (Settings → Secrets and variables → Actions → Variables).
#
# Onde encontrar: painel do Supabase → Project Settings → API
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...sua-chave-anon-public
